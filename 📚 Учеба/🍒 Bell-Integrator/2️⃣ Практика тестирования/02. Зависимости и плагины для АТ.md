- [[#Зависимости|Зависимости]]
	- [[#Зависимости#JUnit Jupiter API|JUnit Jupiter API]]
		- [[#JUnit Jupiter API#Для чего он нужен?|Для чего он нужен?]]
		- [[#JUnit Jupiter API#Примеры аннотаций JUnit Jupiter API|Примеры аннотаций JUnit Jupiter API]]
	- [[#Зависимости#JUnit Jupiter Params|JUnit Jupiter Params]]
		- [[#JUnit Jupiter Params#Для чего он нужен?|Для чего он нужен?]]
		- [[#JUnit Jupiter Params#Аннотации|Аннотации]]
	- [[#Зависимости#JUnit Jupiter Engine|JUnit Jupiter Engine]]
		- [[#JUnit Jupiter Engine#Для чего он нужен?|Для чего он нужен?]]
	- [[#Зависимости#Selenium Java|Selenium Java]]
		- [[#Selenium Java#Для чего используется Selenium Java?|Для чего используется Selenium Java?]]
		- [[#Selenium Java#Что входит в selenium-java?|Что входит в selenium-java?]]
	- [[#Зависимости#Allure JUnit 5|Allure JUnit 5]]
		- [[#Allure JUnit 5#Для чего используется?|Для чего используется?]]
		- [[#Allure JUnit 5#Как это работает в связке с JUnit 5|Как это работает в связке с JUnit 5]]
		- [[#Allure JUnit 5#Пример использования аннотаций Allure JUnit 5|Пример использования аннотаций Allure JUnit 5]]
		- [[#Allure JUnit 5#Пример шагов и аттачей|Пример шагов и аттачей]]
	- [[#Зависимости#Commons IO - позже|Commons IO - позже]]
	- [[#Зависимости#Owner - позже|Owner - позже]]
	- [[#Зависимости#AspectJ Weaver - позже|AspectJ Weaver - позже]]
- [[#Плагины|Плагины]]
	- [[#Плагины#Allure Maven Plugin|Allure Maven Plugin]]
		- [[#Allure Maven Plugin#Для чего нужен?|Для чего нужен?]]
	- [[#Плагины#Maven Compiler Plugin|Maven Compiler Plugin]]
	- [[#Плагины#Maven Surefire Plugin|Maven Surefire Plugin]]

---
## Зависимости

### JUnit Jupiter API

> *JUnit Jupiter API* — это основной модуль JUnit 5, содержащий аннотации (`@Test`, `@BeforeEach`, т.д.) и методы Assertions.  
> 
> Он позволяет **писать** тесты, **но не запускать** их — запуск выполняет Jupiter Engine.

```xml
<dependency>  
    <groupId>org.junit.jupiter</groupId>  
    <artifactId>junit-jupiter-api</artifactId>  
    <version>${junit.jupiter.version}</version>  
</dependency>
```

***JUnit Jupiter API*** — это **основная библиотека JUnit 5**, которая содержит:

✔ аннотации
(чтобы обозначать тесты и жизненный цикл)

✔ методы-ассерты
(проверки результата)

✔ интерфейсы и базовые классы
(для написания тестов)

Это «сердце» JUnit 5 — без него невозможно писать тесты.

#### Для чего он нужен?

- **объявлять тесты** через `@Test`
    
- **выполнять код** до/после тестов (`@BeforeEach`, `@AfterEach`)
    
- **группировать** (`@Nested`)
    
- **отключать тесты** (`@Disabled`)
    
- использовать **утверждения** (`Assertions.*`)
    
- использовать **условия** (`Assumptions.*`)

#### Примеры аннотаций JUnit Jupiter API

```java
@Test                       // тестовый метод
@BeforeEach                 // перед каждым тестом
@AfterEach                  // после каждого теста
@BeforeAll                  // перед ВСЕМИ тестами (static)
@AfterAll                   // после ВСЕХ тестов (static)
@DisplayName("My Test")     // читаемое название
@Disabled                   // отключение теста
```


---

### JUnit Jupiter Params

> *JUnit Jupiter Params* — модуль JUnit 5, который добавляет параметризованные тесты.  
> 
> Он позволяет выполнять один тест с множеством входных данных через аннотации `@ParameterizedTest`, `@ValueSource`, `@CsvSource`, `@MethodSource` и др.  
> 
> Используется для экономии кода и улучшения покрытия тестов.

```xml
<!-- Parameterized Tests -->  
<dependency>  
    <groupId>org.junit.jupiter</groupId>  
    <artifactId>junit-jupiter-params</artifactId>  
    <version>${junit.jupiter.version}</version>  
</dependency>
```

#### Для чего он нужен?

- проверять одну и ту же логику на **множестве значений**
    
- использовать **таблицы данных**
    
- загружать данные из CSV, enum, списков
    
- передавать **сложные типы** в тесты

Этот модуль расширяет возможности базового JUnit Jupiter API.

#### Аннотации

✔`@ParameterizedTest` заменяет `@Test`, если тест должен запускаться много раз.

📘 Пример:

```java
@ParameterizedTest
@ValueSource(strings = {"a", "bb", "ccc"})
void testLength(String s) {
    Assertions.assertTrue(s.length() > 0);
}
```
Этот тест выполнится **3 раза**, с разными значениями.

✔ `@ValueSource` — источники данных: примитивы, строки, классы
```java
@ValueSource(ints = {1, 2, 3})
```

✔`@EnumSource` — значения enum
```java
@EnumSource(MyEnum.class)
```

✔ `@CsvSource` — строки, разделённые запятыми
```java
@CsvSource({"a,1", "bb,2"})
```

✔ `@CsvFileSource` — CSV-файл из ресурсов
```java
@CsvFileSource(resources = "/data.csv")
```

✔ `@MethodSource` — данные из метода
```java
@MethodSource("data")
```

✔ `@ArgumentsSource` — свой источник аргументов
(используется редко, но бывает)


---

### JUnit Jupiter Engine

> *JUnit Jupiter Engine* — это модуль, который выполняет тесты JUnit 5.  
> 
> Он запускает методы с аннотациями `@Test`, управляет жизненным циклом тестов, и передаёт результаты в Maven или IDE.  
> 
> API — для написания тестов, Engine — для их выполнения.

```xml
<!-- Test engine(s) -->  
<dependency>  
    <groupId>org.junit.jupiter</groupId>  
    <artifactId>junit-jupiter-engine</artifactId>  
    <version>${junit.jupiter.version}</version>  
</dependency>
```

#### Для чего он нужен?

- **запускает** методы, помеченные `@Test`
    
- **обрабатывает аннотации** (`@BeforeEach`, `@AfterEach`, `@ParameterizedTest`)
    
- управляет жизненным циклом тестов
	
- **создает** экземпляры тестовых классов и **собирает** их в память
    
- **передаёт результаты** в Maven Surefire или IDE
    
- **интегрируется** с Allure, Gradle, JUnit Platform

Без него тесты просто “не видны” системе. И следовательно не смогут запуститься.


---

### Selenium Java

> *Selenium Java* — это библиотека для автоматизации веб-тестирования.  
> 
> Она позволяет **управлять браузером** через WebDriver: открывать страницы, находить элементы, кликать, вводить текст, работать с окнами и ожиданиями.  
> 
> Это основа UI-автоматизации на Java.

```xml
<!-- https://mvnrepository.com/artifact/org.seleniumhq.selenium/selenium-java -->  
<dependency>  
    <groupId>org.seleniumhq.selenium</groupId>  
    <artifactId>selenium-java</artifactId>  
    <version>${seleniumhq.selenium}</version>  
</dependency>
```

#### Для чего используется Selenium Java?

✔ **Автоматизация веб-тестирования**
Клики, ввод текста, переходы по ссылкам, проверки UI.

✔ **Управление браузерами**
Chrome, Firefox, Edge, Safari и др.

✔ **Получение информации со страницы**
DOM, атрибуты элементов, текст, свойства.

✔ **UI взаимодействие**
- формы
- выпадающие списки
- радиокнопки
- drag & drop
- алерты
- окна и вкладки

✔ **Работа с JS**
Через `JavascriptExecutor`.

#### Что входит в selenium-java?

✔ **WebDriver API**
Интерфейс, который определяет, как управлять браузером:
```java
WebDriver driver = new ChromeDriver();
```

✔ **Классы драйверов**
(сами Java-объекты, управляющие браузером):
- `ChromeDriver`
- `FirefoxDriver`
- `EdgeDriver`

✔ **API поиска элементов**
```java
driver.findElement(By.id("username"));
```

**✔ WebElement — объекты элементов на странице**
```java
WebElement button = driver.findElement(By.id("btn"));
button.click();
```

**✔ Actions API (сложные действия)**
```java
new Actions(driver)
    .moveToElement(elem)
    .click()
    .perform();
```

**✔ Wait API (явные и неявные ожидания)**
- ***Неявное ожидание*** — глобальный таймаут для поиска элементов. Сколько максимум ждать, пока элемент появится в DOM.
    
- ***Явное ожидание*** — ожидание конкретного условия (видимость, кликабельность, текст и т.д.) для конкретного элемента через `WebDriverWait`.
```java
new WebDriverWait(driver, 10)
        .until(ExpectedConditions.visibilityOf(elem));
```

**✔ Менеджменты: окна, куки, фреймы**
```java
driver.manage().window().maximize();
driver.switchTo().frame(0);
driver.manage().deleteAllCookies();
```


---

### Allure JUnit 5

> `allure-junit5` — это **адаптер** между JUnit 5 и Allure.  
> 
> Он слушает выполнение тестов, собирает результаты и формирует `allure-results` в json-формате, на основе которых генерируется HTML-отчёт.  
> 
> С его помощью используются аннотации `@Epic`, `@Feature`, `@Story`, `@Step`, `@Attachment` и т.д., чтобы сделать отчёт структурированным и читаемым.

```xml
<!-- https://mvnrepository.com/artifact/io.qameta.allure/allure-junit5 -->  
<dependency>  
    <groupId>io.qameta.allure</groupId>  
    <artifactId>allure-junit5</artifactId>  
    <version>${qameta.allure}</version>  
</dependency>
```

`allure-junit5` — это **адаптер (интеграция)** между:
- фреймворком тестирования ***JUnit 5***
- и системой репортинга ***Allure***

Он “подсаживается” на JUnit 5 и:
- слушает события тестов (старт, шаги, падения, скипы),
- собирает результаты в формате, понятном Allure,
- создаёт **allure-results**, на основе которых потом строится красивый HTML-отчёт.

#### Для чего используется?

**✔ Красивые отчёты с:**
- деревом тестов,
- статусами (passed/failed/broken/skipped),
- шагами,
- аттачами (скриншоты, логи),
- таймингами, историями запусков.

✔ **Аннотации для документации тестов:**
- `@Epic`
- `@Feature`
- `@Story`
- `@Owner`
- `@Severity`
- `@Description`
- `@Link`, `@Issue`

**✔ Интеграцию шагов (`@Step`) и аттачей (`@Attachment`)**

#### Как это работает в связке с JUnit 5

1. Ты пишешь обычные JUnit 5 тесты.
2. Подключаешь зависимость `allure-junit5`.
3. Запускаешь тесты (Maven Surefire / IDE).
4. Во время выполнения адаптер:
    - перехватывает результаты тестов,
    - складывает их в папку `allure-results`.
5. Потом генерируешь отчёт:
```bash
allure serve allure-results
```
или через Maven-плагин:
```bash
mvn allure:serve
```

#### Пример использования аннотаций Allure JUnit 5
```java
import io.qameta.allure.*;
import org.junit.jupiter.api.Test;

@Epic("Авторизация")
@Feature("Логин через форму")
class LoginTests {

    @Test
    @Story("Успешный логин с валидными данными")
    @Owner("Sergey")
    @Severity(SeverityLevel.CRITICAL)
    @Description("Проверяем, что пользователь может войти с корректными логином и паролем")
    void successfulLoginTest() {
        // шаги теста
    }
}
```

#### Пример шагов и аттачей
```java
import io.qameta.allure.Step;
import io.qameta.allure.Attachment;

class AuthSteps {

    @Step("Логин под пользователем {username}")
    void login(String username, String password) {
        // действия логина
    }

    @Attachment(value = "Скриншот при падении", type = "image/png")
    byte[] attachScreenshot(byte[] screenshot) {
        return screenshot;
    }
}
```
В отчёте будут:
- красивое дерево шагов (`login(...)` будет отображаться в UI),
- прикреплённые файлы (скриншоты и т.д.).


---


### Commons IO - позже

```xml
<!--Для скринов-->  
<dependency>  
    <groupId>commons-io</groupId>  
    <artifactId>commons-io</artifactId>  
    <version>2.4</version>  
</dependency>
```

### Owner - позже

```xml
<!-- Для пропертей -->  
<dependency>  
    <groupId>org.aeonbits.owner</groupId>  
    <artifactId>owner</artifactId>  
    <version>${aeonbits.owner.version}</version>  
</dependency>
```

### AspectJ Weaver - позже

> Нужен для того, чтобы отображать шаги при использовании `@Steps`
> *@Bell Integrator*

```xml
<!-- https://mvnrepository.com/artifact/org.aspectj/aspectjweaver -->  
    <dependency>  
        <groupId>org.aspectj</groupId>  
        <artifactId>aspectjweaver</artifactId>  
        <version>${aspectjweaver.version}</version>  
        <scope>runtime</scope>  
    </dependency></dependencies>
```

## Плагины

### Allure Maven Plugin

> *Allure Maven Plugin* — это инструмент для генерации и **просмотра** Allure-отчётов.  
> 
> Он берёт данные из `allure-results`, создаёт **HTML-отчёт** в `target/allure-reports`, и позволяет открыть его командой `mvn allure:serve`.  
> 
> Используется только на уровне сборки, поэтому находится в блоке `<build><plugins>`.

```xml
<plugin>  
    <groupId>io.qameta.allure</groupId>  
    <artifactId>allure-maven</artifactId>  
    <version>2.10.0</version>  
    <configuration>        
	    <reportVersion>${allure.version}</reportVersion>  
        <resultsDirectory>${basedir}/allure-results</resultsDirectory>  
        <reportDirectory>${basedir}/target/allure-reports</reportDirectory>  
    </configuration></plugin>
```

Это **НЕ библиотека** для автотестов, а инструмент для **сборки и отображения отчётов**.

Он находится в блоке `<build><plugins>`, потому что используется **во время сборки** (build-time), а не в коде.

#### Для чего нужен?

Он выполняет две главные задачи:

1. **Сгенерировать HTML-отчёт из результатов тестов**
Тестовый фреймворк (JUnit 5 через `allure-junit5`) создаёт папку: `allure-results/`

В неё попадают:
- JSON-файлы шагов
- информация о тестах
- прикреплённые скриншоты
- статусы тестов

А ***Allure Maven Plugin*** превращает это в красивый **HTML-отчёт**.


2. **Открыть отчёт в браузере локально**
Команда:
```bash
mvn allure:serve
```
— запускает встроенный веб-сервер и открывает отчёт в браузере.

3. **Генерировать отчёт в папку target**

Команда:
```bash
mvn allure:report
```

Создаёт:
```bash
target/allure-reports/index.html
```

Эту папку можно:
- загрузить в CI (Jenkins, GitLab CI, TeamCity),
- отправить в Telegram,
- хранить артефактом.


---


### Maven Compiler Plugin

> *Maven Compiler Plugin* — основной плагин, который компилирует Java-код.  
> 
> Он управляет версией языка через `<source>` и `<target>`, вызывает `javac`, компилирует main и test директории.  
> 
> Используется практически в каждом Maven-проекте и контролирует, под какую JDK собирается приложение.

```xml
<plugin>  
    <groupId>org.apache.maven.plugins</groupId>  
    <artifactId>maven-compiler-plugin</artifactId>  
    <version>3.8.1</version>  
    <configuration>       
	    <source>9</source>  
        <target>9</target>  
    </configuration>
</plugin>
```

### Maven Surefire Plugin

> *Maven Surefire Plugin* — это плагин, который запускает тесты при команде `mvn test`.  
> 
> Он управляет обнаружением тестов, их выполнением, передачей аргументов JVM, созданием отчётов и интеграцией с тестовыми движками.  
> 
> В автоматизации UI и API он используется всегда.

``` xml
<plugin>  
    <groupId>org.apache.maven.plugins</groupId>  
    <artifactId>maven-surefire-plugin</artifactId>  
    <version>3.0.0-M5</version>  
    <configuration>        
	    <argLine>            -javaagent:"${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar"  
        </argLine>  
    </configuration>   
    <dependencies>        
	    <dependency>            
		    <groupId>org.aspectj</groupId>  
            <artifactId>aspectjweaver</artifactId>  
            <version>${aspectj.version}</version>  
        </dependency>    
    </dependencies>
</plugin>
```

После запуска `mvn test`:

1. Surefire ищет классы с тестами
    - JUnit 5 (`@Test`)
    - TestNG (`@Test`)
    - JUnit 4 (`@Test`)
        
2. Передаёт выполнение соответствующему тестовому движку  
    (например Jupiter Engine)
    
3. Выполняет тесты