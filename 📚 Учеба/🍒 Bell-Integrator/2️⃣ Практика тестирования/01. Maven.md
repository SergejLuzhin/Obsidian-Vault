- [[#Maven|Maven]]
	- [[#Maven#Файл pom.xml|Файл pom.xml]]
	- [[#Maven#Для чего нужен `pom.xml`?|Для чего нужен `pom.xml`?]]
	- [[#Maven#Структура `pom.xml` (коротко)|Структура `pom.xml` (коротко)]]
	- [[#Maven#Установка зависимостей. Блок `<dependencies>`|Установка зависимостей. Блок `<dependencies>`]]
		- [[#Установка зависимостей. Блок `<dependencies>`#Типы зависимостей|Типы зависимостей]]
	- [[#Maven#Установка плагинов. Блок `<build>`|Установка плагинов. Блок `<build>`]]
- [[#Важные примечания|Важные примечания]]
---
## Maven

***Maven*** —  это **система сборки и управления зависимостями** в Java.  
Она автоматически скачивает библиотеки, управляет версиями, компилирует проект, запускает тесты и собирает итоговый JAR.

Он помогает автоматически:

**🔹 1. Управлять зависимостями**

Не нужно вручную скачивать библиотеки — Maven сам загрузит нужные версии из репозитория.

**🔹 2. Собрать проект**

Компиляция, запуск тестов, упаковка в JAR/WAR — всё делает одной командой.

**🔹 3. Управлять структурой проекта**

Maven навязывает стандартную структуру каталогов, чтобы все проекты были одинаковыми.

**🔹 4. Автоматизировать рутинные задачи**

Через плагины: запуск тестов, генерация отчётов, сборка документации и т.п.

### Файл pom.xml

`pom.xml` — это ***главный конфигурационный файл*** Maven-проекта.  
Аббревиатура: POM — Project Object Model.

Этот файл описывает:

✔ структуру проекта
✔ его зависимости
✔ плагины (например, тестирование, сборка)
✔ версию Java
✔ название артефакта (JAR/WAR)
✔ репозитории, откуда скачивать библиотеки
✔ профили (для разных окружений)
Короче: `pom.xml` — это **“мозг” проекта на Maven**.

### Для чего нужен `pom.xml`?

1. **Указать зависимости**, которые Maven должен скачать (например, JUnit, Selenium, Lombok).
    
2. **Настроить сборку проекта** — какую JDK использовать, какие плагины.
    
3. **Описание проекта** — имя, версия, артефакт.
    
4. **Упрощение работы** — вместо ручного управления .jar-файлами.

### Структура `pom.xml` (коротко)

| **Раздел**       | **Для чего нужен**                                  |
| ---------------- | --------------------------------------------------- |
| `<groupId>`      | группа проекта (обычно домен)                       |
| `<artifactId>`   | название артефакта / JAR                            |
| `<version>`      | версия приложения                                   |
| `<dependencies>` | *список библиотек*                                  |
| `<build>`        | *плагины, настройки JDK, фазы сборки*               |
| `<repositories>` | источники зависимостей                              |
| `<properties>`   | общие настройки, например `<maven.compiler.source>` |
| `<profiles>`     | разные конфигурации: dev / prod                     |

### Установка зависимостей. Блок `<dependencies>`

В Maven зависимости подключаются в блоке `<dependencies>...</dependecies>`:

```xml
<dependencies>
    <dependency>
        <groupId>org.seleniumhq.selenium</groupId>
        <artifactId>selenium-java</artifactId>
        <version>4.14.0</version>
    </dependency>
</dependencies>

```

После добавления Maven автоматически:

✔ скачивает нужные библиотеки
✔ помещает их в локальный кэш (`.m2` папка)
✔ подключает к проекту

Никаких ручных скачиваний `.jar`!

#### Типы зависимостей

Каждая зависимость может иметь **область видимости**:

| scope        | объяснение                                                           |
| ------------ | -------------------------------------------------------------------- |
| **compile**  | по умолчанию, используется везде                                     |
| **test**     | используется только в тестах (например, JUnit)                       |
| **provided** | доступна при компиляции, но не включается в jar (например, сервлеты) |
| **runtime**  | нужна только при запуске                                             |
Либо **можно не указывать** scope, чтобы библиотека была видна везде

---

### Установка плагинов. Блок `<build>`

`<build>` — это раздел `pom.xml`, в котором описывается:

✔ как собирать проект
✔ какие плагины использовать
✔ какие директории считать исходниками

> Блок `<build>` в `pom.xml` отвечает за процесс сборки проекта:  
> здесь настраиваются плагины (компилятор, тесты, упаковка), версии Java, структура каталогов и параметры итогового артефакта.  
> 
> Основное, что там обычно указывают — `maven-compiler-plugin` и плагины для запуска тестов и создания JAR.



## Важные примечания



