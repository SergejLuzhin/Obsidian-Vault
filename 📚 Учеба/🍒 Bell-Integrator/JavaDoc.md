- [[#Что такое JavaDoc|Что такое JavaDoc]]
- [[#Для чего нужен JavaDoc|Для чего нужен JavaDoc]]
- [[#Как писать JavaDoc|Как писать JavaDoc]]
- [[#Примеры JavaDoc|Примеры JavaDoc]]
- [[#Теги JavaDoc|Теги JavaDoc]]
- [[#Как генерировать HTML-документацию|Как генерировать HTML-документацию]]
- [[#Краткая сводка|Краткая сводка]]

---
## Что такое JavaDoc

***JavaDoc*** — это стандартный инструмент в Java, который позволяет:

- писать **документацию прямо в коде**, в специальных комментариях;
    
- автоматически **генерировать HTML-документацию**;
    
- удобно показывать подсказки в IntelliJ IDEA, Eclipse, VS Code.

Это официальная система документации Java — всю документацию JDK пишет именно JavaDoc.

## Для чего нужен JavaDoc

1. **Описывать классы, методы и поля**
    Чтобы другим разработчикам было понятно, что делает код.
    
2. **Улучшать читаемость проекта**
    Для новых людей в проекте JavaDoc = инструкция.
    
3. **Объяснять ограничения, входные данные, выходные данные, возможные ошибки**.
    
4. **Помогает тестировщикам (в т.ч. автомейшен)**:
    - понять назначение методов,
    - быстрее писать тесты,
    - видеть контракт каждого метода.
    
5. **Генерировать официальный документированный API.**

## Как писать JavaDoc

JavaDoc — это **комментарий** вида:
```java
/**
 * текст документации
 */
```
Перед классом, методом, интерфейсом, конструктором или полем.

Чтобы его сделать достаточно написать `/**` и нажать Enter.

## Примеры JavaDoc

Перед полями:
```java
public class Order {

    /** Unique identifier of the order. */
    private long id;

    /** Email of the customer who placed the order. */
    private String customerEmail;

    /** Total price of the order in USD. */
    private double totalPrice;
}
```

Перед методом:
```java
/**
 * Calculates the sum of two integers.
 *
 * @param a first number
 * @param b second number
 * @return the sum of a and b
 */
public int sum(int a, int b) {
    return a + b;
}
```

Перед классом:
```java
/**
 * Service for working with users.
 * Provides operations for creating, updating and deleting users.
 */
public class UserService {
    ...
}
```

Для конструкторов:
```java
/**
 * Creates a user with a given name and age.
 *
 * @param name user's name
 * @param age user's age
 */
public User(String name, int age) {
    ...
}
```

Для интерфейса или абстрактного метода:
```java
/**
 * Filters data according to some rules.
 *
 * @param data input list
 * @return filtered list
 */
List<String> filter(List<String> data);
```
## Теги JavaDoc

| Тег             | Значение                                           |
| --------------- | -------------------------------------------------- |
| **@param**      | описание входного параметра                        |
| **@return**     | что возвращает метод                               |
| **@throws**     | какие исключения может бросить                     |
| **@since**      | с какой версии                                     |
| **@deprecated** | помечает метод устаревшим / с какой версии устарел |
| **@see**        | ссылка на другой класс/метод                       |
| **@author**     | автор кода                                         |
| **@version**    | версия                                             |
📘 Пример с несколькими тегами:
```java
/**
 * Finds user by ID.
 *
 * @param id unique identifier
 * @return User object if found, null otherwise
 * @throws IllegalArgumentException if id is negative
 * @see UserService#getAllUsers()
 */
public User findById(int id) { ... }
```

## Как генерировать HTML-документацию

В IntelliJ IDEA:
**Tools → Generate JavaDoc…**

После генерации получится красивая HTML-документация:
```java
/docs/index.html
```

## Краткая сводка

> “JavaDoc — это встроенный инструмент Java для создания документации в формате HTML.
> 
> Он позволяет описывать классы, методы, параметры и возвращаемые значения.
> 
> IDE показывает JavaDoc как подсказку, что делает работу с кодом удобнее и безопаснее.”