- [[#Что такое @Step|Что такое @Step]]
- [[#Для чего нужна|Для чего нужна]]
- [[#Подходы использования Степов|Подходы использования Степов]]
	- [[#Подходы использования Степов#1. Шаги в PageObject|1. Шаги в PageObject]]
	- [[#Подходы использования Степов#Шаги в отдельном классе|Шаги в отдельном классе]]
	- [[#Подходы использования Степов#3. Шаги прямо в тесте|3. Шаги прямо в тесте]]
- [[#Дополнение: переопределения Assertions|Дополнение: переопределения Assertions]]
- [[#Кратко|Кратко]]
---
## Что такое @Step

`@Step` — это *аннотация* из библиотеки **Allure**.  

Используется для того, чтобы:

✔ красиво оформлять шаги теста в Allure-отчёте
✔ логировать (документировать) действия автотеста
✔ делать отчёт **понятным** для тестировщика, разработчика, менеджера

## Для чего нужна

1) **Улучшает читаемость Allure-отчётов**  
    Отчёт превращается в понятные бизнес-шаги:
    
    - “Авторизоваться в системе”
        
    - “Создать новую задачу”
        
    - “Проверить статус”
        
2) **Помогает искать причину падения**  
    Падает шаг — видно, на каком именно.
    
3) **Отделяет логику теста от технических деталей**  
    Тесты становятся “сценариями”, а не набором вызовов WebDriver.
    
4)  **Шаги могут принимать аргументы — они показываются в отчёте**  
    Например:  
    _Ввод логина: user123._
    
5) Используются **в PageObject-подходе**  
    Логику действий можно описывать шагами прямо в PageObject.

## Подходы использования Степов

### 1. Шаги в PageObject

Каждый публичный метод Page Object → это шаг.

Плюсы:  
✔ тесты короткие  
✔ отчёты читаемые  
✔ логика скрыта внутри PageObject

### Шаги в отдельном классе

Подход схож с [[Obsidian-Vault/📚 Учеба/🍒 Bell-Integrator/1️⃣ Теория тестирования/01. Тестирование#^505948|BDD]]. В самих тестах описывается лишь поведение, вся логика выполнения инкапсулирована.

```java
public void testOpenV2WithStepsAll(String searchQuery, String title, String moneyFirst, String moneySecond) {  
    openSite("https://www.google.ru/","Google",chromeDriver);  
    searchInGoogle(searchQuery);  
    validateSearchInGoogle(title);  
    goSiteByLink(title);  
    checkCourse(moneyFirst,moneySecond);  
}
```

А уже в самом классе Steps:
```java
@Step("Переходим на сайт: {url}")  
public static void openSite(String url, String title, WebDriver currentDriver){  
    driver=currentDriver;  
    driver.get(url);  
    wait = new WebDriverWait(driver,30);  
    wait.until(ExpectedConditions.titleIs(title));  
}
// и так далее ...
```

### 3. Шаги прямо в тесте

Степы подписываются внутри самих тестов.

Данный подход обычно **запрещен** в продакшене, так как засоряет сам тест, но его могут использовать новички.

## Дополнение: переопределения Assertions

Частая практика - *переопределить ассерты* (точнее создать метод с таким же именем в собственном классе) и добавить аннотацию @Step перед ними, чтобы они **отображались в Allure** не только тогда, когда они не проходят.

```java
public class Assertions {  
  
    @Step("Проверяем что нет ошибки: '{message}'")  
    public static void assertTrue(boolean condition, String message){  
        org.junit.jupiter.api.Assertions.assertTrue(condition, message);  
    }  
}
```
## Кратко

>`@Step` — *аннотация Allure* для создания шагов в отчёте.  
>
>Делает тесты понятными, показывает параметры шагов, помогает искать причину падения.  
>
>Используется в PageObject или в классе Steps.  
>
>Позволяет создавать красивые и читаемые отчёты для команды.