
---

## DesiredCapabilities

*DesiredCapabelities* - класс в Selenium, с помощью которого можно прописывать желаемые возможности для Веб-драйвера.

Грубо говоря:

> «Я хочу такой-то браузер, такой версии, на такой платформе, вот с такими особыми фишками» — и всё это упаковывается в `DesiredCapabilities`.

📘Пример. Отключение ожидания загрузки страницы

```java
DesiredCapabilities capabilities = new DesiredCapabilities();  
capabilities.setCapability(CapabilityType.PAGE_LOAD_STRATEGY,"none");
```

При использовании данного исключения, необходимо использовать больше явных ожиданий на конкретные веб-элементы, чтобы все работало корректно.

Чтобы данные настройки работали, необходимо передать созданные capabilities в конструктор объекта Веб-драйвера.

```java
chromeDriver = new ChromeDriver(capabilities);
```


## Проперти для тестов

Обычно их сохраняют в пакете `resources` в файле `test.properties`

Внутри он выглядит примерно так:

```
bellintegrator.url=https://bellintegrator.ru/  
default.timeout=30
```

### Библиотека для работы с properties

Для работы с пропертями используется библиотека *aeonbits*

```xml
<dependency>  
    <groupId>org.aeonbits.owner</groupId>  
    <artifactId>owner</artifactId>  
    <version>${aeonbits.owner.version}</version>  
</dependency>
```

### Интерфейс для взаимодействия с пропертями

Для взаимодействия с пропертями необходимо написать *интерфейс*:

```java
@Config.LoadPolicy(Config.LoadType.MERGE)  
@Config.Sources({"system:properties",  
        "system:env",  
        "file:src/main/resources/tests.properties"  
})  
public interface TestsProperties extends Config {  
  
    @Config.Key("bellintegrator.url")  
    String bellIntegratorUrl();  
  
    @Config.Key("default.timeout")  
    int defaultTimeout();  
}
```

_@Config.Sources_ - источники, где aeonbits будет искать проперти
_@Config.Key_ - ключ для поиска пропертей

### Класс прослойка

Для удобства использования пропертей можно также создать отдельный класс:

```java
import org.aeonbits.owner.ConfigFactory;  
  
public class Properties {  
    public static TestsProperties testsProperties = ConfigFactory.create(TestsProperties.class);  
}
```

И затем использовать его в коде:
```java
chromeDriver.manage().timeouts().implicitlyWait(testsProperties.defaultTimeout(), TimeUnit.SECONDS);

public void testProps() {  
    System.out.println(testsProperties.bellIntegratorUrl());
```

*Классов и интерфейсов может быть несколько, в зависимости от проекта.*
