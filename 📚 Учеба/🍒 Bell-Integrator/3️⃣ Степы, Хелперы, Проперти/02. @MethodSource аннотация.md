- [[#Что такое @MethodSource|Что такое @MethodSource]]
- [[#Для чего используется|Для чего используется]]
- [[#Пример провайдера аргументов|Пример провайдера аргументов]]
- [[#Кратко|Кратко]]
---

## Что такое @MethodSource

`@MethodSource` — это ***аннотация JUnit 5*** для **параметризованных тестов**.

Она говорит JUnit:

> «Возьми данные для этого теста **из метода** (или методов), который(е) вернёт(ут) набор аргументов».

Используется **вместе** с `@ParameterizedTest`.

``` java
@ParameterizedTest(name = "{displayName}: {arguments}")  
@MethodSource("helpers.DataProvider#providerCheckingMoney")
```

Структура указания нужного метода следующая:

```java
"пакет . Класс # метод"
```


## Для чего используется

Чтобы:

- запускать **один и тот же тест много раз** с разными входными данными;
    
- хранить тестовые данные **в коде** (а не в аннотации `@CsvSource` или в файле);
    
- генерировать данные программно, с логикой (циклы, условия, коллекции и т.д.).

Подходит, когда:

- нужно несколько аргументов
    
- нужны сложные объекты (не только строки/числа)
    
- данные удобно собирать в отдельном методе

📘 Примеры:

1) Создать и заполнить лист в методе и затем передать его
2) Считать данные из файла в методе и передать полученные данные
3) Подключиться к базе данных и передать нужные элементы

## Пример провайдера аргументов

```java
public static Stream<Arguments> providerCheckingMoney(){  
    return Stream.of(  
            Arguments.of("курсы валют", "Официальные курсы валют на заданную дату ...", "Лари","Азербайджанский манат")//,  
}
```

*Метод-источник* должен возвращать `Stream` , коллекцию или массив значений.

## Кратко

> `@MethodSource` — это аннотация JUnit 5 для параметризованных тестов. 
> 
> Она указывает, что данные для теста нужно взять из метода-источника, который возвращает `Stream`, коллекцию или массив значений.  
> 
> Удобна, когда нужны сложные входные данные, несколько аргументов, либо данные, которые удобно сгенерировать программно.