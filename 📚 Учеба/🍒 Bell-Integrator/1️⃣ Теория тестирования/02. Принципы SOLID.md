
---
## Что такое принципы SOLID

Принципы ***SOLID*** — это набор из **5 правил проектирования** в объектно-ориентированном программировании, которые помогают писать **понятный, гибкий, легко поддерживаемый и расширяемый код**.

| Принцип | Полное название                                                             | Суть в 1 фразе                                                                                                | Плохой пример (нарушение)                                        | Хороший пример (правильно)                                                      |
| ------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| **SRP** | ***Single Responsibility Principle***<br><br>Принцип единой ответственности | **Одна ответственность — один класс.** Класс должен иметь только одну причину для изменения.                  | Класс UserService и сохраняет пользователя, и отправляет письма. | Разделение на UserService и EmailService.                                       |
| **OCP** | ***Open/Closed Principle***<br><br>Принцип открытости / закрытости          | **Открыт для расширения, закрыт для изменения.** Новое поведение — через новые классы, а не изменение старых. | Класс Discount с кучей if (type == ...), растущий бесконечно.    | Интерфейс Discount, отдельные классы NewDiscount, VipDiscount и т.д.            |
| **LSP** | ***Liskov Substitution Principle***<br><br>Принцип подстановки Лисков       | **Подкласс должен полностью заменять родителя.** Не ломать поведение, не нарушать ожидания.                   | Penguin наследует Bird и переопределяет fly() исключением.       | Разделение интерфейсов: Bird и FlyingBird, пингвин не реализует fly.            |
| **ISP** | ***Interface Segregation Principle***<br><br>Принцип разделения интерфейса  | **Лучше много маленьких интерфейсов, чем один большой.**                                                      | Интерфейс Worker с методами work(), drive(), cook().             | Разделение на Workable, Drivable, Cookable.                                     |
| **DIP** | ***Dependency Inversion Principle***<br><br>Принцип инверсии зависимостей   | **Зависимость от абстракций, а не реализаций.**                                                               | NotificationService напрямую зависит от EmailSender.             | NotificationService зависит от интерфейса Sender (EmailSender, TelegramSender). |

### Для чего нужны

В теории, при соблюдении данных принципов код:

* легко расширять
* легко изменять
* легко поддерживать
* легко читать
* легко тестировать

### Нюансы

**Знание SOLID не научит вас программировать!** Более того, в реальных проектах практически никогда нет постоянного соблюдения всех пяти принципов.

*Золотая середина* - стиль написания кода **между** Хард-кодом и Софт-кодом.

*Хард-код* - вся программа написана сверху вниз, без разбиения на сущности, такую программу легко написать, но практически невозможно изменять и расширять.

*Софт-код* - программа разбита на отдельные сущности-классы, каждый из которых выполняет строго свою задачу. Если переусерствовать, то будет тяжело разобраться в устройстве такой программы.

> Любой дурак может написать код понятный компьютеру. Но только хороший программист может написать код понятный человеку.
> *@ Мартин Фаулер*

---
## SRP - Принцип единой ответственности

Каждый класс должен иметь **только одну причину для изменения**.

Один функционал / сущности должны быть в одном классе, а не размазаны по всей программе.

📘 Пример:
Если класс отвечает за интеграцию с Excel, то он должен меняться только при изменении Excel.

Если проще:
👉 **один класс = одна задача**
👉 **один модуль = одна ответственность**

### Примеры:

🌧 Плохой пример (нарушение SRP):
```java
class UserService {

    void saveUser(User user) {
        // сохраняем пользователя в БД
    }

    void sendWelcomeEmail(User user) {
        // отправляем письмо
    }

    void logUserInfo(User user) {
        // пишем лог
    }
}
// Данный класс и сохраняет пользователя, и отправляет email, и логирует
```

☀️ Хороший пример (правильно по SRP):
```java
class UserRepository {
    void save(User user) { ... }
}

class EmailService {
    void sendWelcomeEmail(User user) { ... }
}

class LoggerService {
    void logUserInfo(User user) { ... }
}
// Каждый класс выполняет **одну чётко определённую задачу**
```

### Применимо к АТ

Необходимо **инкапсулировать структуру страницы от логики выполнения тестов**. Для того, чтобы при изменении структуры страницы не нужно было по отдельности переписывать все тесты. (а их может быть тысяча и больше)

Класс структуры страницы называется - *Page Object*.

Пейдж обжекты также должны содержать **только одну страницу**.

---
### Частые ошибки и уловки на собеседованиях

**Ошибка 1**
«Но класс может выполнять две похожие функции, это же нормально?»

➡ Ответ:
Нет. Даже похожие функции — разные причины для изменения.

---
 **Ошибка 2**
«SRP означает 1 метод в классе».

➡ Нет! Класс может иметь много методов — но все о **одной задаче**.

---
 **Ошибка 3**
«Можно объединить всё в один сервис, это удобно».

➡ Удобно до первого изменения, потом — ад.