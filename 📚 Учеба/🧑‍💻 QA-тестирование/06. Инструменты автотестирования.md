- [[#Основные инструменты автоматизированного тестирования на Java|Основные инструменты автоматизированного тестирования на Java]]
	- [[#Основные инструменты автоматизированного тестирования на Java#1. JUnit / TestNG — фреймворки для написания тестов|1. JUnit / TestNG — фреймворки для написания тестов]]
	- [[#Основные инструменты автоматизированного тестирования на Java#2. Selenium / Selenide — тестирование UI (веб-интерфейса)|2. Selenium / Selenide — тестирование UI (веб-интерфейса)]]
	- [[#Основные инструменты автоматизированного тестирования на Java#3. REST Assured — тестирование API|3. REST Assured — тестирование API]]
	- [[#Основные инструменты автоматизированного тестирования на Java#4. Allure Report — отчёты о тестировании|4. Allure Report — отчёты о тестировании]]
	- [[#Основные инструменты автоматизированного тестирования на Java#5. Maven / Gradle — системы сборки и управления зависимостями|5. Maven / Gradle — системы сборки и управления зависимостями]]
	- [[#Основные инструменты автоматизированного тестирования на Java#6. Git / GitHub — контроль версий|6. Git / GitHub — контроль версий]]
	- [[#Основные инструменты автоматизированного тестирования на Java#7. CI/CD (например, Jenkins, GitHub Actions)|7. CI/CD (например, Jenkins, GitHub Actions)]]
- [[#Мини-конспект / Краткая памятка для собеседования|Мини-конспект / Краткая памятка для собеседования]]

---
## Основные инструменты автоматизированного тестирования на Java

**Инструменты тестировщика-автоматизатора** — это то, на чём держится практическая часть профессии.

---
### 1. JUnit / TestNG — фреймворки для написания тестов

Это основа любой автоматизации на Java. С их помощью мы **описываем, запускаем и проверяем тесты**.

📘 **JUnit** — самый популярный, простой и современный (версия 5).
📘 **TestNG** — аналог с расширенными возможностями (например, группировка тестов, зависимости).

 🔹 Что они делают:
- Позволяют писать тесты в виде обычных Java-классов.
- Проверяют, совпадает ли ожидаемый результат с фактическим.
- Формируют отчёты о прохождении тестов.

```java
// Пример на JUnit 5
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class LoginTest {

    @Test
    void successfulLogin() {
        String actual = "Welcome, user!";
        String expected = "Welcome, user!";
        assertEquals(expected, actual);
    }
}
```
🧠 Результат: если actual ≠ expected, тест упадёт — значит, есть дефект.

💬 На собеседовании:
> «JUnit используется для написания и запуска юнит- и интеграционных тестов на Java.»

---
### 2. Selenium / Selenide — тестирование UI (веб-интерфейса)

Эти инструменты позволяют **управлять браузером** автоматически: открывать страницы, нажимать кнопки, вводить текст, проверять элементы.

 **🔹 Selenium WebDriver**
- Работает напрямую с браузером (Chrome, Firefox, Safari и др.)
- Требует больше кода и ручной настройки ожиданий.

 **🔹 Selenide**
- Надстройка над Selenium, которая делает код проще и стабильнее.

> «Selenide — это удобный инструмент для автоматизации UI, он упрощает работу с браузером и делает тесты стабильными.»

---
### 3. REST Assured — тестирование API

Используется для проверки **бэкенд-эндпоинтов** без интерфейса. Помогает убедиться, что сервер возвращает правильные ответы.

```java
import static io.restassured.RestAssured.*;
import static org.hamcrest.Matchers.*;

import org.junit.jupiter.api.Test;

public class ApiTest {

    @Test
    void getUserTest() {
        given()
            .baseUri("https://api.myshop.ru")
        .when()
            .get("/users/1")
        .then()
            .statusCode(200)
            .body("name", equalTo("Ivan"));
    }
}
```
🧠 Проверяет: сервер отвечает 200 OK, и имя пользователя — “Ivan”.

> «REST Assured — это библиотека для автоматизации тестирования REST API на Java.»

---
### 4. Allure Report — отчёты о тестировании

 Allure создаёт **наглядные HTML-отчёты** о результатах тестов:
 какие прошли, какие упали, с логами, скриншотами и временем выполнения.

 💡 Как выглядит:
- Графики успешных и проваленных тестов
- Скриншоты неудачных шагов
- Пошаговые сценарии  

📘 Обычно подключается как плагин к JUnit/TestNG и CI (например, Jenkins).

> «Allure используется для визуализации результатов автотестов, чтобы команда могла быстро увидеть состояние качества продукта.»

---
### 5. Maven / Gradle — системы сборки и управления зависимостями

Эти инструменты управляют **зависимостями** проекта (библиотеками) и **запуском тестов**.

**🔹 Maven:**
- Использует файл pom.xml
- Прост, часто применяется в проектах QA

**🔹 Gradle:**
- Современнее и быстрее
- Использует build.gradle

> «Maven и Gradle управляют зависимостями проекта и позволяют запускать тесты через команды сборки.»

---
### 6. Git / GitHub — контроль версий

 Git позволяет **сохранять историю изменений кода**, работать в команде, создавать ветки, проверять коммиты, делать ревью.

```bash
git clone <url>      # скопировать проект
git add .            # добавить изменения
git commit -m "Add new test"
git push origin main # отправить в репозиторий
```

> «Git — обязательный инструмент. Он используется для командной работы и хранения тестового кода на GitHub.»

---
### 7. CI/CD (например, Jenkins, GitHub Actions)

 Эти системы **автоматически запускают тесты** после каждого изменения в коде.
 Это нужно, чтобы сразу увидеть, если кто-то “сломал” проект.

📘 Пример сценария:
1. Разработчик сделал push в GitHub
2. GitHub Actions запускает все JUnit и UI тесты
3. Результаты отправляются в Allure Report
4. Если тесты упали — команда получает уведомление

> «CI/CD — это автоматический запуск тестов и деплоя, чтобы контролировать качество при каждом изменении кода.»

---
## Мини-конспект / Краткая памятка для собеседования

Экосистема тестов на Java:
```bash
Java
 ├── JUnit / TestNG       → структура и запуск тестов
 ├── Selenide / Selenium  → UI-тестирование
 ├── REST Assured         → API-тестирование
 ├── Allure               → отчётность
 ├── Maven / Gradle       → управление зависимостями
 ├── Git / GitHub         → хранение кода
 └── Jenkins / Actions    → автоматический запуск тестов (CI/CD)
```

> «Для автоматизации на Java я использую JUnit или TestNG для написания тестов,
> Selenide — для UI, REST Assured — для API.
> Все зависимости управляю через Maven, отчёты собираю в Allure, а тесты запускаю автоматически через Jenkins или GitHub Actions.»

