- [[#Что такое Java Collection Framework (JCF)|Что такое Java Collection Framework (JCF)]]
- [[#Иерархия коллекций|Иерархия коллекций]]
	- [[#Иерархия коллекций#1. Интерфейс Collection|1. Интерфейс Collection]]
		- [[#1. Интерфейс Collection#List— список (упорядоченная коллекция)|List— список (упорядоченная коллекция)]]
		- [[#1. Интерфейс Collection#Set — множество (уникальные элементы)|Set — множество (уникальные элементы)]]
		- [[#1. Интерфейс Collection#Queue — очередь (FIFO: первый вошёл — первый вышел)|Queue — очередь (FIFO: первый вошёл — первый вышел)]]
	- [[#Иерархия коллекций#2. Интерфейс Map — хранение пар «ключ → значение»|2. Интерфейс Map — хранение пар «ключ → значение»]]
- [[#Мини-конспект / Краткая памятка для собеседования|Мини-конспект / Краткая памятка для собеседования]]

---
## Что такое Java Collection Framework (JCF)

*Java Collection Framework* — это набор **интерфейсов**, **классов** и **алгоритмов**, которые позволяют хранить, упорядочивать и обрабатывать группы объектов.

Проще говоря:

> Коллекции — это **контейнеры для данных**, более удобные, чем обычные массивы (Array).

## Иерархия коллекций

Коллекции делятся на **две большие группы**:
1. *Collection* — хранит элементы (списки, множества, очереди).
2. *Map* — хранит пары “ключ → значение”.

### 1. Интерфейс Collection

*Collection* — это корневой интерфейс для всех коллекций, кроме Map.

От него наследуются **три основных интерфейса**:
```bash
Collection
 ├── List
 ├── Set
 └── Queue
```

#### List— список (упорядоченная коллекция)

  Хранит элементы **в порядке добавления**, допускает **дубликаты**.

 Основные реализации:

| Класс          | Особенности                                             | Когда использовать                       |
| -------------- | ------------------------------------------------------- | ---------------------------------------- |
| **ArrayList**  | Динамический массив                                     | Быстрый доступ по индексу, частое чтение |
| **LinkedList** | Двусвязный список                                       | Частые вставки/удаления в середине       |
| **Vector**     | Синхронизированный ArrayList                            | Старый, почти не используется            |
| **Stack**      | Наследник Vector, LIFO (последний вошёл — первый вышел) | Узкоспециализирован                      |
📘 Пример:
```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");
list.add("Java");
System.out.println(list); // [Java, Python, Java]
```
💡 Дубликаты разрешены, порядок сохраняется.

#### Set — множество (уникальные элементы)

  Не хранит дубликаты. Порядок может быть неважен или определяться правилами сортировки.

| Класс         | Особенности                                | Когда использовать                 |
| ----------------- | ---------------------------------------------- | -------------------------------------- |
| **HashSet**       | На основе HashMap, порядок не гарантирован     | Быстрое добавление/поиск, уникальность |
| **LinkedHashSet** | Запоминает порядок вставки                     | Когда нужен порядок + уникальность     |
| **TreeSet**       | Отсортированное множество (использует TreeMap) | Когда нужен порядок по возрастанию     |
📘 Пример:
```java
Set<String> set = new HashSet<>();
set.add("QA");
set.add("Java");
set.add("QA");
System.out.println(set); // [QA, Java] — дубликат отброшен
```
💡 Нет дубликатов, порядок не гарантирован.

#### Queue — очередь (FIFO: первый вошёл — первый вышел)

  Используется, когда важен порядок обработки элементов (например, задачи, сообщения и т.п.).

| Класс         | Особенности                       | Когда использовать                          |
| ----------------- | ------------------------------------- | ----------------------------------------------- |
| **LinkedList**    | Реализует и List, и Queue             | Очередь с возможностью добавлять в начало/конец |
| **PriorityQueue** | Элементы сортируются по приоритету    | Планировщики, сортировка по значению            |
| **ArrayDeque**    | Быстрая двухсторонняя очередь (Deque) | Альтернатива Stack/LinkedList                   |
📘 Пример:
```java
Queue<String> queue = new LinkedList<>();
queue.add("Task1");
queue.add("Task2");
System.out.println(queue.poll()); // Task1
```
💡 FIFO: первым пришёл — первым ушёл.

### 2. Интерфейс Map — хранение пар «ключ → значение»

*Map* не наследуется от Collection, но является частью JCF. 
Ключи уникальны, значения могут повторяться.

Основные реализации:

| Класс             | Особенности                      | Когда использовать             |
| --------------------- | ------------------------------------ | ---------------------------------- |
| **HashMap**           | Неупорядочен, основан на хэш-таблице | Быстрый доступ по ключу            |
| **LinkedHashMap**     | Сохраняет порядок вставки            | Когда важен порядок                |
| **TreeMap**           | Отсортирован по ключам               | Когда нужен порядок по возрастанию |
| **Hashtable**         | Старый синхронизированный вариант    | Практически не используется        |
| **ConcurrentHashMap** | Потокобезопасная версия              | Для многопоточности                |
📘 Пример:
```java
Map<Integer, String> map = new HashMap<>();
map.put(1, "Java");
map.put(2, "Python");
map.put(1, "Go");
System.out.println(map); // {1=Go, 2=Python}
```
💡 Ключи уникальны, значения можно перезаписывать.

## Мини-конспект / Краткая памятка для собеседования

| Интерфейс | Уникальность | Упорядоченность | Дубликаты        | Пример реализации |
| ------------- | ---------------- | ------------------- | -------------------- | --------------------- |
| **List**      | ❌                | ✅ (по индексу)      | ✅                    | ArrayList             |
| **Set**       | ✅                | ⚠️ (иногда)         | ❌                    | HashSet               |
| **Queue**     | ❌                | ✅ (по очереди)      | ✅                    | LinkedList            |
| **Map**       | ✅ (по ключам)    | ⚠️ (иногда)         | Ключи ❌ / Значения ✅ | HashMap               |
> «В Java коллекции делятся на Collection и Map. 
> *Collection* включает List, Set и Queue, а *Map* хранит пары “ключ–значение”.
> Для списка используется ArrayList, для уникальных данных — HashSet, а для быстрого доступа по ключу — HashMap.»