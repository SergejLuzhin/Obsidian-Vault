
---
## Что такое Stream API

*Stream API* — это инструмент в Java, который позволяет работать с коллекциями **в декларативном стиле**, как с **потоком данных**.

Проще говоря:
> Stream — это **“конвейер операций”**, через который проходят элементы коллекции, и над ними выполняются преобразования, фильтрация, сортировка, поиск и т.д.

## Три идеи Stream API

### **1️⃣** Поток ленивый (lazy)

Промежуточные операции (filter, map, sorted…) _не выполняются сразу_.

Код просто **описывает конвейер**.

Реальная работа начинается только при **терминальной операции** (collect, count, findFirst…).

### **2️⃣** Поток одноразовый

После терминальной операции поток закрывается — повторно использовать его нельзя.

```java
Stream<String> s = list.stream();
s.count();     // OK
s.count();     // Ошибка! Stream has already been operated upon
```

### **3️⃣** Stream НЕ хранит данные

Он _не является коллекцией_ и _не меняет исходную коллекцию_.

Все изменения делаются над “проекцией” данных.

Пример:
```java
List<Integer> numbers = List.of(1, 2, 3);
numbers.stream().map(n -> n * 2);
// numbers осталось неизменным
```

## Виды операций в Streams

### 🔹 Промежуточные (не запускают вычисления)

*Возвращают новый stream*
#### filter()

Оставляет только те элементы, для которых условие = true.
```java
List<Integer> list = List.of(1, 2, 3, 10, 11);
List<Integer> result = list.stream()
        .filter(x -> x > 5)
        .collect(Collectors.toList());
//[10, 11]
```

#### map()

Преобразует каждый элемент в **другой** элемент.
```java
List<String> names = List.of("anna", "bob");
List<String> upper = names.stream()
        .map(String::toUpperCase)
        .collect(Collectors.toList());
// ["ANNA", "BOB"]
```
    
#### flatMap()
**Самый важный метод.**

Делает две вещи:
1. превращает один элемент в поток элементов;
2. **расплющивает** (flatten) всё в один общий поток.

Пример №1: список списков → список
```java
List<List<Integer>> data = List.of(
        List.of(1, 2),
        List.of(3, 4)
);

List<Integer> flat = data.stream()
        .flatMap(list -> list.stream())
        .collect(Collectors.toList());
        
// [1, 2, 3, 4]
```

Пример №2: строки → слова:
```java
List<String> lines = List.of(
    "java is nice",
    "i love streams"
);

List<String> words = lines.stream()
        .flatMap(line -> Arrays.stream(line.split(" ")))
        .collect(Collectors.toList());
        
// [java, is, nice, i, love, streams]
```

#### sorted() / sorted(comparator)

Сортировка потока
```java
List<Integer> sorted = List.of(3,1,2).stream()
        .sorted()
        .collect(Collectors.toList());
// [1, 2, 3]
```
    
#### distinct()

Удаляет повторяющиеся элементы:
```java
List<Integer> unique = List.of(1,2,2,3).stream()
        .distinct()
        .collect(Collectors.toList());
// [1, 2, 3]
```

#### limit() и skip()

- limit(n) — взять первые n элементов.
- skip(n) — пропустить первые n элементов.

```java
List<Integer> res = List.of(1,2,3,4,5).stream()
        .skip(2)
        .limit(2)
        .collect(Collectors.toList());
// [3, 4]
```

---
### 🔹 Терминальные (завершают поток)

*Возвращают число, объект, коллекцию или void.*

#### collect()

Собирает поток обратно в список, множество или карту.

#### count()

Считает элементы
```java
long c = List.of(1,2,3).stream().count();
//3
```

#### forEach()

Применяет **функцию к каждому элементу потока**. Ничего не возвращает (void).

Зачем нужен:
- вывести элементы на экран;
- залогировать данные;
- сделать побочный эффект (например, сложить в внешний список).
``
```java
List<String> names = List.of("Anna", "Bob", "Kate");

names.stream()
     .forEach(name -> System.out.println("Имя: " + name));
     
    //Имя: Anna
	//Имя: Bob
	//Имя: Kate
```

#### findFirst() / findAny()

Возвращает первый или любой элемент (Optional).

#### anyMatch() / allMatch() / noneMatch()

```java
boolean ok = List.of(1,2,3).stream()
        .anyMatch(x -> x > 2);
// true
```

#### min() / max()

Ищут **минимальный** или **максимальный** элемент в потоке.
Возвращают `Optional<T>`.
  
У них есть две формы:

1. Поток Comparable-типов (например, Integer, String) — без компаратора:
```java
Optional<Integer> min = list.stream().min(Integer::compareTo);
```

2. С объектами (через Comparator)
```java
class User {
    int age;
    String name;
    User(int age, String name) { this.age = age; this.name = name; }
}

List<User> users = List.of(
    new User(30, "Max"),
    new User(20, "Anna"),
    new User(25, "Kate")
);

Optional<User> youngest = users.stream()
        .min(Comparator.comparingInt(u -> u.age));

System.out.println(youngest.get().name); // Anna
```
#### reduce()

Складывает все в одно значение
```java
int sum = List.of(1,2,3).stream()
        .reduce(0, Integer::sum);
// 6
```

---
## Базовые примеры

Исходный список:
```java
List<String> names = List.of("Anna", "Bob", "Alexander", "Alina", "Tom");
```

### Фильтр (filter)

Оставить только имена с длиной > 3:
```java
List<String> result = names.stream()
        .filter(name -> name.length() > 3)
        .collect(Collectors.toList());
```

### Преобразование (map)
```java
List<String> upper = names.stream()
        .map(String::toUpperCase)
        .collect(Collectors.toList());
```

### Удаление дубликатов
```java
List<Integer> nums = List.of(1, 2, 2, 3, 3, 3);

List<Integer> unique = nums.stream()
        .distinct()
        .collect(Collectors.toList());
```

### Сортировка
```java
List<String> sorted = names.stream()
        .sorted()
        .collect(Collectors.toList());
```
- sorted() без параметров → сортирует по **возрастанию**. (в данном случае идет сравнение по коду символов)

### Преобразование списка объектов в Map
```java
class User {
    int id;
    String name;

    User(int id, String name) { this.id = id; this.name = name; }
}

List<User> users = List.of(
        new User(1, "Max"),
        new User(2, "Kate")
);

Map<Integer, String> map = users.stream()
        .collect(Collectors.toMap(
                user -> user.id,
                user -> user.name
        ));
```

## Сложные концепции Streams (простым языком)

### Разница map vs flatMap

map: один элемент → один элемент
flatMap: один элемент → _много элементов_ (или другой поток), с “распрямлением”.

Пример: есть список списков
```java
List<List<Integer>> data = List.of(
        List.of(1, 2),
        List.of(3, 4)
);
```

Если сделать:
```java
data.stream().map(list -> list)
```
→ будет `Stream<List>`

Чтобы сделать Stream:
```java
List<Integer> flat = data.stream()
        .flatMap(list -> list.stream())
        .collect(Collectors.toList());
// [1, 2, 3, 4]
```


## Как подставлять свои функции и компараторы

### 1. Свой компаратор в sorted()

Пример: сортировка строк по длине:
```java
List<String> names = List.of("Anna", "Bob", "Alexander", "Tom");

List<String> sortedByLength = names.stream()
        .sorted((a, b) -> Integer.compare(a.length(), b.length()))
        .collect(Collectors.toList());

System.out.println(sortedByLength);
// [Bob, Anna, Tom, Alexander]
```

Пример: сортировка пользователей по возрасту, а потом по имени:
```java
List<User> users = List.of(
    new User(30, "Max"),
    new User(20, "Anna"),
    new User(30, "Alex")
);

List<User> sortedUsers = users.stream()
        .sorted(
            Comparator
                .comparingInt((User u) -> u.age)   // сначала по возрасту
                .thenComparing(u -> u.name)        // потом по имени
        )
        .collect(Collectors.toList());
```

### 2. Собственные функции в filter(), map(), forEach()

Можно вынести логику в отдельный метод и передать его ссылкой (::).

Пример: свой метод-фильтр
```
boolean isAdult(User u) {
    return u.age >= 18;
}

List<User> adults = users.stream()
        .filter(this::isAdult)  // передаём СВОЮ функцию
        .collect(Collectors.toList());
```

Пример: свой метод для преобразования
```java
boolean isAdult(User u) {
    return u.age >= 18;
}

List<User> adults = users.stream()
        .filter(this::isAdult)  // передаём СВОЮ функцию
        .collect(Collectors.toList());
```

### Что насчет count() со своей логикой?

count() **сам по себе** не принимает функцию — он просто считает элементы потока.

Но можно применить свою логику до count():

Пример: посчитать, сколько пользователей старше 25
```java
long count = users.stream()
        .filter(u -> u.age > 25)  // твой предикат
        .count();

System.out.println(count); // например, 2
```

Или через вынесенный метод:
```java
boolean isSenior(User u) { return u.age > 25; }

long count = users.stream()
        .filter(this::isSenior)
        .count();
```

---
## Мини-конспект / Краткая памятка для собеседования

> «Stream API — это конвейер лениво выполняемых операций над данными.
> 
> Есть промежуточные операции (filter, map, flatMap, sorted…), 
> и терминальные (collect, count, forEach…).
> 
> map — один элемент в один; flatMap — один в набор элементов.
> toMap требует уникальных ключей.
> 
> Поток одноразовый и не изменяет исходную коллекцию.»

*Промежуточные операции* (не запускают вычислений):

| Метод                   | Что делает                                       | Пример                                |
| ----------------------- | ------------------------------------------------ | ------------------------------------- |
| **filter(predicate)**       | Оставляет элементы, где условие true             | .filter(x -> x > 10)                  |
| **map(function)**           | Преобразует элемент в другой элемент             | .map(String::toUpperCase)             |
| **flatMap(function)**       | Превращает один элемент в поток и “расплющивает” | .flatMap(list -> list.stream())       |
| **sorted() / sorted(comp)** | Сортирует поток                                  | .sorted(Comparator.comparingInt(...)) |
| **distinct()**              | Удаляет дубликаты                                | .distinct()                           |
| **limit(n)**                | Берёт первые n элементов                         | .limit(5)                             |
| **skip(n)**                 | Пропускает первые n элементов                    | .skip(2)                              |

*Терминальные операции* (запускают выполнение):

| Метод                                 | Что делает                               |
| ------------------------------------- | ---------------------------------------- |
| **collect()**                             | Собирает поток (в List, Set, Map и т.д.) |
| **count()**                               | Возвращает количество элементов          |
| **forEach()**                             | Выполняет действие над каждым элементом  |
| **findFirst() / findAny()**               | Возвращают Optional с элементом          |
| **anyMatch() / allMatch() / noneMatch()** | Проверки-предикаты                       |
| **min(comp) / max(comp)**                 | Находит min/max (возвращает Optional)    |
| **reduce()**                              | Сворачивает поток в одно значение        |

