- [[#1. ArrayList - динамический массив|1. ArrayList - динамический массив]]
	- [[#1. ArrayList - динамический массив#При первом add()|При первом add()]]
	- [[#1. ArrayList - динамический массив#Алгоритм добавления (add(E e))|Алгоритм добавления (add(E e))]]
	- [[#1. ArrayList - динамический массив#Удаление (remove(index))|Удаление (remove(index))]]
	- [[#1. ArrayList - динамический массив#⚡ Быстродействие|⚡ Быстродействие]]
- [[#2. LinkedList— двусвязный список|2. LinkedList— двусвязный список]]
	- [[#2. LinkedList— двусвязный список#Добавление элемента ( add(E e) )|Добавление элемента ( add(E e) )]]
	- [[#2. LinkedList— двусвязный список#Удаление (remove(Node n))|Удаление (remove(Node n))]]
	- [[#2. LinkedList— двусвязный список#⚡ Быстродействие|⚡ Быстродействие]]
	- [[#2. LinkedList— двусвязный список#🧠 На собеседовании могут спросить:|🧠 На собеседовании могут спросить:]]
- [[#3. HashMap - хэш-таблица|3. HashMap - хэш-таблица]]
	- [[#3. HashMap - хэш-таблица#🚀 Алгоритм вставки (put(K key, V value))|🚀 Алгоритм вставки (put(K key, V value))]]
	- [[#3. HashMap - хэш-таблица#Удаление (remove(Object key))|Удаление (remove(Object key))]]
	- [[#3. HashMap - хэш-таблица#⚡ Быстродействие|⚡ Быстродействие]]
	- [[#3. HashMap - хэш-таблица#🧠 На собеседовании могут спросить:|🧠 На собеседовании могут спросить:]]
- [[#TreeMap - Крсно-черное дерево|TreeMap - Крсно-черное дерево]]
	- [[#TreeMap - Крсно-черное дерево#⚙️ Вставка (put)|⚙️ Вставка (put)]]
	- [[#TreeMap - Крсно-черное дерево#🧹 Удаление (remove)|🧹 Удаление (remove)]]
	- [[#TreeMap - Крсно-черное дерево#⚡ Быстродействие|⚡ Быстродействие]]
	- [[#TreeMap - Крсно-черное дерево#🧠 На собеседовании могут спросить:|🧠 На собеседовании могут спросить:]]
- [[#Итоговая схема механизмов|Итоговая схема механизмов]]
- [[#Мини-конспект / Краткая памятка для собеседования|Мини-конспект / Краткая памятка для собеседования]]

---
## 1. ArrayList - динамический массив

*ArrayList* — это **обёртка над обычным массивом**, который автоматически **расширяется** при переполнении.

Он реализует интерфейс List и обеспечивает **быстрый доступ по индексу**, но **медленные вставки в середину**.

🔍 Внутренние поля
```java
transient Object[] elementData; // массив для хранения элементов
private int size;                // текущее количество элементов
```

 🚀 Конструкторы

| Конструктор                            | Что происходит внутри                                                                                          |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **new ArrayList()**                    | создаётся пустой список, массив elementData ещё **не создаётся** вообще (ленивая инициализация при первом add) |
| **new ArrayList(int initialCapacity)** | создаётся массив нужной ёмкости сразу                                                                          |
| **new ArrayList(Collection c)**        | копирует элементы из другой коллекции                                                                          |
📘 Интересный факт:
>В Java 8+ ArrayList() по умолчанию **не выделяет память** до первого add().
>Реально создаётся массив DEFAULTCAPACITY_EMPTY_ELEMENTDATA, **размером 0**.

### При первом add()

Если коллекция пуста (вызван конструктор без аргументов):
```java
elementData = new Object[10];
```
👉 Стартовая ёмкость — 10 элементов.

---
###  Алгоритм добавления (add(E e))

1. Проверяется, хватает ли места:
```java
ensureCapacityInternal(size + 1);
```

2. Если места нет → вызывается grow(), где:
```java
int newCapacity = oldCapacity + (oldCapacity >> 1);
```
👉 то есть **новый размер = старый × 1.5** (рост на 50%).

>Коротко: oldCapacity >> 1 — это **арифметический сдвиг вправо на 1 бит** в Java, т.е. по сути **деление на 2** с округлением вниз.
>
>Для неотрицательных чисел: эквивалентно oldCapacity / 2.
>Пример: 10 >> 1 = 5, 5 >> 1 = 2.

3. Копирование старого массива в новый через Arrays.copyOf().
4. Элемент записывается в конец elementData[size++].

 📘 Пример
```java
Изначально: []
→ add("A")  → массив становится [A, null, null, ..., null] (10 мест)
→ add("B")  → [A, B, null, ..., null]
...
→ при переполнении (на 11-м элементе) → создаётся новый массив длиной 15
```

---
###  Удаление (remove(index))

1. Проверяется диапазон индекса.
2. Удаляемый элемент сохраняется для возврата.
3. Все элементы справа **сдвигаются влево**:
```java
System.arraycopy(elementData, index + 1, elementData, index, size - index - 1);
```

4. Последний элемент обнуляется (null), чтобы GC мог освободить память.
5. size--.

---
### ⚡ Быстродействие

| Операция      | Время                 | Пояснение                         |
| ------------- | --------------------- | --------------------------------- |
| **get(index)**    | O(1)                  | прямой доступ                     |
| **add()**         | амортизированное O(1) | копирование при переполнении O(n) |
| **remove(index)** | O(n)                  | сдвиг элементов                   |
| **contains()**    | O(n)                  | линейный поиск                    |
💡 *“Амортизированная” сложность* — т.к. копирование происходит не на каждое добавление, а лишь при переполнении массива.

---
## 2. LinkedList— двусвязный список

*LinkedList* — это **цепочка узлов (Node)**, где каждый элемент знает своего предыдущего и следующего соседа.

```java
private static class Node<E> {
    E item;
    Node<E> next;
    Node<E> prev;
}
```

Поля в *LinkedList*:
```java
transient int size = 0;
transient Node<E> first;
transient Node<E> last;
```

---
###  Добавление элемента ( add(E e) )

1. Создаётся новый узел Node(prev=last, item=e, next=null).
2. Если список пуст → first и last указывают на этот элемент.
3. Иначе:
    - last.next = newNode
    - newNode.prev = last
    - last = newNode
4. size++.

Кратко:
>При добавлении элемента создаётся новый узел, который привязывается в конец списка. Если список пуст — он становится первым, иначе новый узел соединяется с предыдущим, и размер списка увеличивается на 1.

---
### Удаление (remove(Node n))

1. У prev и next обновляются связи:
```java
if (prev == null) first = next; else prev.next = next;
if (next == null) last = prev; else next.prev = prev;
```

2. n.item, n.next, n.prev = null (для GC).
3. size--.

### ⚡ Быстродействие

| Операция                | Сложность | Комментарий                |
| ----------------------- | --------- | -------------------------- |
| **addFirst, addLast**       | O(1)      | без прохода                |
| **removeFirst, removeLast** | O(1)      |                            |
| **get(index)**              | O(n)      | нужно пройти n/2 элементов |
| **add(index, e)**           | O(n)      | поиск узла + вставка       |
💡 В отличие от ArrayList, LinkedList **не нужно расширять**, но **доступ по индексу медленнее**.

### 🧠 На собеседовании могут спросить:

- ❓ _Как хранится LinkedList внутри?_
    👉 Как двусвязная цепочка Node(prev, next).
    
- ❓ _Почему доступ по индексу O(n)?_
    👉 Потому что приходится идти по цепочке от first или last.
    
- ❓ _Как он экономит память?_
    👉 Не экономит — наоборот, тратит больше, так как хранит 2 указателя на каждый элемент.

---

## 3. HashMap - хэш-таблица

*HashMap* — это **массив бакетов** (Node<K,V>[] table), где каждый бакет хранит связанный список или дерево (Node или TreeNode).

*Бакет (bucket)* — это **ячейка внутреннего массива HashMap**, в которой хранятся элементы с одинаковыми хэшами.

>В бакете может быть несколько элементов (цепочка) , если у разных ключей получился **одинаковый индекс** (*коллизия*).

 🔍 Структура данных
 ```java
 static class Node<K,V> implements Map.Entry<K,V> {
    final int hash;
    final K key;
    V value;
    Node<K,V> next;
}
 ```

### 🚀 Алгоритм вставки (put(K key, V value))

1. Вычисляется hash:
```java
int hash = (key == null) ? 0 : key.hashCode() ^ (key.hashCode() >>> 16);
```
 — это **расширенное хэширование**, чтобы равномерно распределить хэши по битам.

2. Определяется индекс в массиве:
```java
index = (n - 1) & hash;
```
— побитовое И вместо % (быстрее, при n = 2^k).

3. Если бакет пуст → вставляем новый узел.

4. Если в бакете уже есть элементы:
    - сравниваем hash и equals(key) —
        если совпадают → обновляем значение;
    - если нет → добавляем новый узел в конец цепочки.

5. Если длина цепочки > 8 → **преобразуется в дерево** (TreeNode — красно-чёрное дерево).

6. Если заполненность > loadFactor * capacity (по умолчанию 0.75 * 16 = 12)
    → **resize():** массив удваивается, все элементы перераспределяются.

💡 Чуть проще и кратко:
>При добавлении пары ключ–значение put(K,V) в HashMap сначала вычисляется хэш ключа и находится место в массиве, где он должен лежать.
>
>Если ячейка пуста — туда кладётся новый элемент.
>
>Если в ней уже что-то есть — сравниваются ключи: при совпадении обновляется значение, иначе элемент добавляется в цепочку.
>
>Если цепочка становится слишком длинной, она превращается в дерево для ускорения поиска.
>
>Когда карта заполняется больше чем на 75%, она автоматически расширяется вдвое.

---

### Удаление (remove(Object key))

1. Вычисляем hash и индекс.
    
2. Проходим цепочку бакета.
    
3. Если найден equals(key) — удаляем узел, пересвязываем next.

Коротко: 
> remove(key) ищет пару по ключу и, если находит, вырезает узел из своего «бакета», уменьшает размер и возвращает старое значение; если не находит — возвращает null.


### ⚡ Быстродействие
| Операция               | Средняя сложность | Худшая                                    |
| ---------------------- | ----------------- | ----------------------------------------- |
| **put/get/remove**     | O(1)              | O(n) при множестве коллизий               |
| **С Java 8+ (дерево)** | O(1)              | O(log n) если цепочки превращены в дерево |
### 🧠 На собеседовании могут спросить:

- ❓ _Что происходит при коллизии?_
    👉 Элементы с одинаковым индексом образуют цепочку (или дерево).
    
- ❓ _Когда HashMap расширяется?_
    👉 При size > capacity * loadFactor (по умолчанию 0.75).
    
- ❓ _Почему размер всегда степень двойки?_
    👉 Чтобы быстро вычислять индекс через (n - 1) & hash.
    
- ❓ _Зачем “деревья” в Java 8+?_
    👉 Чтобы ускорить поиск в длинных цепочках (с O(n) до O(log n)).

## TreeMap - Красно-черное дерево

TreeMap хранит ключи **в отсортированном порядке**.

В основе — **сбалансированное красно-чёрное дерево**.

### ⚙️ Вставка (put)

1. Если дерево пустое — новый элемент становится корнем.
    
2. Сравнение через compareTo() (натуральный порядок) или Comparator.
    
3. Вставляем слева/справа.
    
4. Если нарушается баланс дерева — выполняется **поворот** (левый/правый) и перекраска узлов.

### 🧹 Удаление (remove)

- Если у узла два потомка — ищем “следующего по порядку” (in-order successor).
    
- Заменяем удаляемый.
    
- Балансируем дерево.

### ⚡ Быстродействие

| Операция       | Сложность  | Комментарий            |
| -------------- | ---------- | ---------------------- |
| **put/get/remove** | O(log n)   | благодаря балансировке |
| **Сортировка**     | O(n log n) | уже встроена           |
### 🧠 На собеседовании могут спросить:

- ❓ _В чём отличие HashMap и TreeMap?_
    👉 HashMap быстрее (O(1)), TreeMap медленнее (O(log n)), но хранит ключи **отсортированно**.
    
- ❓ _Что произойдёт при одинаковых ключах?_
    👉 Старое значение будет перезаписано.
    
- ❓ _Можно ли использовать null в TreeMap?_
    👉 Нет, NullPointerException, потому что compareTo() не умеет сравнивать null.

## Итоговая схема механизмов

| Коллекция  | Внутренняя структура | Механизм добавления                            | Особенности                           |
| -------------- | ------------------------ | -------------------------------------------------- | ----------------------------------------- |
| **ArrayList**  | Массив Object[]          | Добавление в конец, при переполнении — grow() ×1.5 | Стартовая ёмкость 10                      |
| **LinkedList** | Узлы (Node)              | Добавление путём перестройки ссылок                | Быстрое добавление, медленный поиск       |
| **HashMap**    | Хэш-таблица (Node[])     | hash → bucket → цепочка или дерево                 | Быстрый поиск, resize при loadFactor>0.75 |
| **TreeMap**    | Красно-чёрное дерево     | Сравнение ключей и балансировка                    | Хранит элементы отсортированно            |

---
## Мини-конспект / Краткая памятка для собеседования

> «ArrayList использует массив, начальная ёмкость 10, при переполнении увеличивается в 1.5 раза.

> LinkedList — двусвязный список, каждый элемент хранит ссылки на предыдущий и следующий.

> HashMap — массив бакетов, ключи ищутся по hashCode и equals, при переполнении массив удваивается, а цепочки могут стать деревом.

> TreeMap реализован как красно-чёрное дерево, хранит ключи отсортированно, все операции — O(log n).»