
---
## 1. Типы данных, операторы, условия и циклы

### Типы данных

В Java всё **строго типизировано**: переменная имеет определённый тип.

| Категория       | Типы                       | Пример                |
| --------------- | -------------------------- | --------------------- |
| **Примитивные** | int, double, char, boolean | int age = 25;         |
| **Ссылочные**   | String, ArrayList, Object  | String name = "Ivan"; |
💡 _Примитивы_ хранят значение напрямую.
   _Ссылочные_ — содержат ссылку на объект в памяти.

---
### Операторы

Используются для вычислений и логики:

| Тип                | Пример               | Значение         |
| ------------------ | -------------------- | ---------------- |
| **Арифметические** | +, -, *, /, %        | int sum = a + b; |
| **Сравнения**      | ==, !=, >, <, >=, <= | if (a == b)      |
| **Логические**     | &&, !                |                  |
### Условия и циклы

Условие:
```java
if (x > 10) {
    System.out.println("x больше 10");
} else {
    System.out.println("x меньше или равно 10");
}
```

Цикл for:
```java
for (int i = 0; i < 5; i++) {
    System.out.println("Тест " + i);
}
```

Цикл while:
```java
int i = 1;
while (i <= 5) {          // 1,2,3,4,5
    System.out.print(i + " ");
    i++;
}
```

Цикл do while - выполнится хотя бы один раз:
```java
import java.util.Scanner;

Scanner sc = new Scanner(System.in);
int n;
do {
    System.out.print("Введите число 1..10: ");
    n = sc.nextInt();
} while (n < 1 || n > 10);

System.out.println("Ок: " + n);
```

💡 В тестах циклы часто используются для проверки списков элементов на странице или ответов API.

---
## 2. Методы, классы, объекты
### Класс

*Класс* — это “чертёж” объекта. Определяет, какие у него есть **поля (данные)** и **методы (действия)**.

```java 
public class User {
    String name;
    int age;

    public void sayHello() {
        System.out.println("Привет, я " + name);
    }
}
```

### Объект

*Объект* - это **экземпляр класса** (реальный “пользователь” по чертежу). 

```java
User user = new User();
user.name = "Иван";
user.sayHello(); // вывод: Привет, я Иван
```

###  Методы

*Методы* — это действия объекта.

У них есть **параметры**, **возвращаемый тип** и **тело**.

```java
public int sum(int a, int b) {
    return a + b;
}
```

💡 В автотестах методы часто используются как шаги:
например, loginPage.enterUsername("user") или api.getResponse().

## 3. Наследование, интерфейсы и ООП

Java построена на **ООП (объектно-ориентированном программировании)**.
Главная цель — сделать код **структурированным и переиспользуемым**.

### Принципы ООП
#### Инкапсуляция

 Сокрытие внутренней логики от внешнего мира.

📘 Пример:
```java
public class BankAccount {
    private double balance = 0; // нельзя менять напрямую

    public void deposit(double amount) {
        balance += amount;
    }

    public double getBalance() {
        return balance;
    }
}
```
💡 Мы не даём прямого доступа к balance, а управляем им через методы.

#### Наследование

 Один класс **наследует** другой и получает его свойства.

📘 Пример:

```java
public class Animal {
    public void makeSound() {
        System.out.println("Звук животного");
    }
}

public class Dog extends Animal {
    public void makeSound() {
        System.out.println("Гав-гав");
    }
}
```
💡 Dog наследует Animal, но может переопределять методы. Это помогает повторно использовать код.

#### Полиморфизм

 Возможность работать с объектами разных типов через общий интерфейс.

📘 Пример:
```java
Animal animal = new Dog();  // ссылка на Animal, но объект Dog
animal.makeSound(); // вывод: "Гав-гав"
```

💡 В тестах это полезно, когда у нас разные типы драйверов или API,но общий метод, например:
`driver.clickElement()` — работает и для Web, и для Mobile.

#### Абстракция

   Сокрытие деталей реализации, оставление только сути.
   
📘 Пример интерфейса:
```java
public interface Payment {
    void pay(double amount);
}

public class CardPayment implements Payment {
    public void pay(double amount) {
        System.out.println("Оплата картой на " + amount);
    }
}
```
💡 Мы знаем, что Payment умеет “платить”, но не как именно. Это даёт гибкость — можно подставить любую реализацию.

## 4. Исключения (try–catch–finally)

   Исключения нужны для обработки **ошибок во время выполнения** программы.

```java
try {
    int result = 10 / 0;
} catch (ArithmeticException e) {
    System.out.println("Ошибка: деление на ноль");
} finally {
    System.out.println("Этот блок выполнится всегда");
}
```

💡 В тестировании это используется, чтобы **тест не падал при сбое**, а корректно обрабатывал исключение и записывал в отчёт.

## 5. Уровни доступа (Access Modifiers) в Java

   Уровни доступа определяют, **где и кем** может быть использован класс, метод или переменная.

> То есть, насколько “открыт” элемент для внешнего мира.

| Модификатор                     | Видимость           | Где доступен                 | Типичные случаи использования                 |
| ------------------------------- | ------------------- | ---------------------------- | --------------------------------------------- |
| **public**                      | Открыт для всех     | Везде, даже в других пакетах | Общие классы, API, тестовые методы            |
| **protected**                   | Ограниченный доступ | Внутри пакета и наследниках  | Методы, которые должны быть доступны потомкам |
| **default _(package-private)_** | Без модификатора    | Только внутри одного пакета  | Вспомогательные классы и методы               |
| **private**                     | Закрыт              | Только внутри самого класса  | Данные, которые нельзя менять извне           |

| Модификатор   | Доступ в том же классе | В том же пакете | В наследниках | В других пакетах      |
| ------------- | ---------------------- | --------------- | ------------- | --------------------- |
| **public**    | ✅                      | ✅               | ✅             | ✅                     |
| **protected** | ✅                      | ✅               | ✅             | ❌ (кроме наследников) |
| **_default_** | ✅                      | ✅               | ❌             | ❌                     |
| **private**   | ✅                      | ❌               | ❌             | ❌                     |
|               |                        |                 |               |                       |
### public

 Полностью открытый доступ — можно вызывать **откуда угодно**.

🧠 Применение:
- Методы, которые должны быть доступны тестам и другим классам.
- Основные точки входа (public static void main()).

### private

   Самый строгий уровень — доступен **только внутри этого класса.**
   
🧠 Применение:
- Скрытие внутренних полей и вспомогательных методов. (Инкапсуляция)
- Безопасность и контроль доступа к данным.
### protected

 Доступ внутри **того же пакета** и в **классах-наследниках** (даже из других пакетов).
 
🧠 Применение:
- Когда нужно дать потомкам возможность использовать или переопределять метод, но скрыть его от постороннего кода.

### default (package-private)

   Если модификатор не указан — доступ только **в пределах одного пакета**.
   
🧠 Применение:
- Для вспомогательных классов, которые не должны быть видны за пределами пакета.
- Часто используется для внутренних утилит.

---
## Мини-конспект / Краткая памятка для собеседования

| Понятие          | Простой смысл                      | Пример                        |
| ---------------- | ---------------------------------- | ----------------------------- |
| **Класс**        | Чертёж объекта                     | User                          |
| **Объект**       | Экземпляр класса                   | new User()                    |
| **Метод**        | Действие                           | login()                       |
| **Инкапсуляция** | Прячем данные                      | private balance               |
| **Наследование** | Дети наследуют родителей           | Dog extends Animal            |
| **Полиморфизм**  | Один интерфейс — разные реализации | List = ArrayList / LinkedList |
| **Абстракция**   | Оставляем только суть              | interface Payment             |
| **Исключения**   | Контроль ошибок                    | try–catch                     |
> «Я понимаю основы Java: типы данных, условия, циклы, методы и классы. 
> 
> Знаю принципы ООП — инкапсуляцию, наследование, полиморфизм и абстракцию.
> 
> Эти знания нужны, чтобы писать структурированные, устойчивые автотесты.»

